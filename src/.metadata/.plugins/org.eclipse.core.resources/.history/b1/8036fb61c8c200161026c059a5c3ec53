package wu.leizi.Driver;

public class DriverFactory {
	
	public static Driver factory(String ent) {
		if (ent.equals(new )) {
			return new HBClientDriver();
		}
		else {
			return new HBServerDriver();
		}
	}
}
